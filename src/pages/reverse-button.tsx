import { ReverseButton } from "../components/inputs/password-input/reverse-button";

export default function ReverseButtonVariant() {
  return (
    <div>
      <ReverseButton variant="variant1">TEXT</ReverseButton>
      <ReverseButton variant="variant2">TEXT</ReverseButton>
      <ReverseButton variant="variant3">TEXT</ReverseButton>
    </div>
  );
}
